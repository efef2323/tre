services:
  - type: web
    name: telegram-bot-schedule
    env: python
    region: frankfurt  # или oregon, singapore
    plan: starter  # от $7/месяц - МИНИМУМ ДЛЯ СКРИНШОТОВ!
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
      playwright install-deps
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        value: "8531196180:AAHTRMQ1dgNqbdnJM9Cy4ByoCv6FPlzpYsI"
    healthCheckPath: /
    autoDeploy: true